import { Alert } from "../../style/style";

const ImagesListError: React.FC = () => (
  <Alert>
    It was not possible to load the data at this time. Please try again later.
  </Alert>
);

export default ImagesListError;
